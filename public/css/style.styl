// colors
highlight1 = #3266A8
highlight2 = lighten(highlight1, 20%)
background1 = darken(highlight1, 60%)
background2 = darken(highlight1, 20%)
dark = #2e2e2e
light = #eee
borderColor = #ddd
lightText = #eee
darkText = #333


// breakpoints
midBreakPoint = 950px
mobileBreakPoint = 320px

mid = "all and (max-width: " + midBreakPoint + ")"
mobile = "all and (max-width: " + mobileBreakPoint + ")"

fullWidth = 40%
midWidth = 60%
mobileWidth = 100%

*
  box-sizing border-box
  -moz-box-sizing border-box

body
  background background1
  font-size 12pt
  line-height 12pt

header
  background dark
  color lightText
  width fullWidth
  margin auto

  @media mobile
    position fixed
    left 0
    right 0
    z-index 9

  @media mid
    width midWidth

  @media mobile
    width mobileWidth

  .title
    margin 0%
    padding 2%
    background highlight2
    font-size 160%
    font-weight normal
    line-height 90%

    @media mobile
      font-size 130%

  .tagline
    margin 0%
    padding 1% 0%
    font-size 60%
    line-height 100%
    font-weight normal

  .info-bar
    padding 1% 2% 2%
    font-size 70%
    background dark

    a
      color darken(lightText, 30%)
      &:hover
        color darken(lightText, 20%)

    div
      display inline-block

    .credits
      float right
.page
  width 40%
  margin auto
  padding 0% 1%
  background background2
  position absolute
  left 0
  right 0

  @media mid
    width midWidth

  @media mobile
    width mobileWidth
    margin-top 75px


.stream
  background light

  ol.stream-items
    margin 0
    margin-bottom 1%
    padding 0
    list-style-type: none
    
    .stream-item
      background white
      &:hover
        background darken(white, 5%)

    .tweet
      padding 2% 1.5% 5%
      border-bottom 1px solid borderColor

      .content
        margin-left 58px

        .stream-item-header
          padding-bottom 1%

          .account-group
            text-decoration none
            color darkText

            .avatar
              float left
              margin-top 0.5%
              margin-left -56px
              width 48px
              border-radius 10%

            .fullname
              font-weight bold

            .username
              font-size 90%
              font-weight normal
              color lighten(darkText, 30%)

          .time
            // TODO - display this better
            display none

      .tweet-text
        color darkText
        white-space pre-wrap
        word-wrap break-word
        font-size 90%
        line-height 140%
        text-decoration none


// .spotlighted .tweet.minimized
// TODO - USE CLASSES ABOVE INSTEAD OF TESTING NTH CHILD BELOW TO TRIGGER MINIMALIZE STYLES
.stream ol.stream-items .stream-item:nth-child(2) 
  .tweet
    padding-bottom 1%
    line-height 40%
    .content 
      margin-left 30px
      .stream-item-header
        padding-bottom 0
        .account-group
          .avatar
            width 23px
            margin-top 0%
            margin-left -30px
            border-radius 15%
          .fullname
            font-size 70%
          .username
            font-size 60%
      .tweet-text
        font-size 75%
        overflow hidden
        display inline-block
        width 100%
        white-space nowrap
        overflow hidden
        text-overflow ellipsis
